# Tasktracker: Защита от ошибочного выбора номенклатуры и лишних закупок

## Контекст и цель

**Проблема.** В требовании `OR/2026/06/0027` прораб выбрал товар **«Фланец 65-10 кованый»**, потому что он появился в ручной выборке номенклатуры. После расчёта наличия Odoo отправил строку в закупку, так как именно этой карточки не было на складе. При этом подходящая складская позиция **«Фланец DN65, PN 16»** была в наличии, но не попала прорабу в очевидную выборку.

**Цель.** Снизить риск ошибочного сопоставления товара и не давать строкам уходить в закупку, если на складах выдачи есть похожая или допустимая позиция с остатком.

**Ожидаемый результат.**

- Прораб и снабженец видят более релевантные кандидаты: сначала товары с остатком на складах выдачи, затем остальные похожие товары.
- Перед созданием закупки система предупреждает о похожих товарах с остатком.
- Для типовых замен работает управляемый каталог аналогов/допустимых замен.
- Память сопоставлений закрепляет правильные решения для повторяющихся строк.
- Номенклатура постепенно приводится к единому формату, чтобы поиск не зависел от случайных названий.

## Текущее состояние

- Ручной выбор товара в строке требования использует обычное поле `product_id` без домена и без ранжирования по остаткам.
- В модуле уже есть:
  - `object.request.matching.candidate.service` — построение shortlist кандидатов;
  - `object.request.matching.memory` — память подтверждённых сопоставлений;
  - `ai_candidate_product_ids` / `ai_suggested_product_id` — AI-кандидаты;
  - `allowed_substitute_ids` — допустимые замены на уровне строки;
  - `object.request.line.stock` — остатки и план выдачи по складам.
- Расчёт наличия работает по выбранным складам выдачи и смотрит только на текущий `product_id` строки.
- Мастер закупки создаёт PO по строкам `qty_to_buy > 0`, но не проверяет похожие товары с остатком.

## Принятые проектные принципы

| # | Решение | Обоснование |
|---|---------|-------------|
| 1 | Не заменять выбранный товар молча | Пользователь должен явно подтвердить замену, иначе можно выдать не тот материал |
| 2 | Товары с остатком должны быть выше в подсказках | Это снижает вероятность лишней закупки |
| 3 | Закупка должна иметь защитный контроль | Ошибка выбора должна ловиться до создания PO |
| 4 | Аналоги должны быть управляемыми | Не все похожие товары являются допустимой заменой |
| 5 | Нормализация справочника — отдельный долгосрочный этап | Нельзя быстро и безопасно переименовать весь каталог без ревизии |

## Затронутые компоненты

- `custom_addons/object_request/models/object_request.py`
  - подготовка AI-кандидатов;
  - пересопоставление строк;
  - проверка наличия;
  - авто-разбивка;
  - открытие мастера закупки.
- `custom_addons/object_request/models/object_request_line.py`
  - поля сопоставления;
  - допустимые замены;
  - onchange/валидации выбора товара.
- `custom_addons/object_request/models/matching_candidate_service.py`
  - ранжирование кандидатов;
  - поиск похожих товаров;
  - учёт складских остатков.
- `custom_addons/object_request/models/matching_memory.py`
  - память подтверждённых сопоставлений.
- `custom_addons/object_request/wizards/purchase_wizard.py`
  - защита перед созданием закупки.
- `custom_addons/object_request/wizards/stock_check_wizard.py`
  - предупреждения по остаткам и заменам.
- `custom_addons/object_request/views/object_request_views.xml`
  - отображение кандидатов, остатков и предупреждений.
- `custom_addons/object_request/views/object_request_line_views.xml`
  - список строк сопоставления.
- `custom_addons/object_request/security/*`
  - права на подтверждение замен и управление аналогами.
- `docs/project.md`
  - архитектура подбора номенклатуры и складских защит.
- `docs/changelog.md`
  - описание user-visible изменений.

---

## Этап 0. Кодовый контракт правил замены

- **Статус**: Выполнена
- **Описание**: Реализовать в коде базовые правила, по которым система отличает допустимую замену, запрещённую замену и похожий товар, требующий ручного подтверждения.
- **Шаги выполнения**:
  - [x] Добавить сервис/хелпер правил замен, например
    `object.request.substitution.policy` или внутренний helper в
    `matching_candidate_service.py`, который возвращает решение:
    - `allowed_with_confirmation`;
    - `blocked`;
    - `unknown_requires_review`.
  - [x] Реализовать нормализацию технических признаков для фланцев:
    - `Ду`, `DN`, `65мм` → единый номинальный диаметр;
    - `1,0МПа` → `PN10`;
    - `1,6МПа` → `PN16`;
    - `PN 10`, `PN10`, `Ру10` → `PN10`;
    - `PN 16`, `PN16`, `Ру16` → `PN16`.
  - [x] Реализовать правило давления:
    - `PN10` / `1,0МПа` можно заменить на `PN16`;
    - обратная замена `PN16` → `PN10` запрещена как понижение давления.
  - [x] Реализовать базовый подход к признакам:
    - диаметр должен совпадать строго;
    - семейство товара должно совпадать строго: фланец только на фланец,
      кран только на кран, отвод только на отвод и т.д.;
    - `PN` допускается только равный или более высокий;
    - материал, ГОСТ, исполнение и тип соединения при явном конфликте
      запрещают замену;
    - если материал, ГОСТ, исполнение или тип соединения не распознаны,
      замена требует ручного подтверждения снабженца.
  - [x] Интегрировать результат правила в подбор кандидатов и guard закупки:
    - запрещённые кандидаты не должны получать складской бонус;
    - кандидаты `allowed_with_confirmation` могут показываться выше при
      наличии остатка, но не применяются автоматически;
    - причина решения должна попадать в `reason` / warning text.
  - [x] Зафиксировать, что автоматическая выдача аналога без подтверждения
    пользователя на этом этапе технически запрещена.
  - [x] Ограничить право подтверждения замены снабженцем/ответственной ролью
    через существующие группы доступа.
  - [x] Добавить тесты на правила:
    - `Фланец ст. Ду 65мм 1,0МПа` → `Фланец DN65, PN 16` требует
      подтверждения и может быть рекомендован;
    - `Фланец DN65, PN 16` → `Фланец ст. Ду 65мм 1,0МПа` блокируется;
    - `DN65` не заменяется на `DN40`;
    - фланец не заменяется на кран/отвод, кран не заменяется на отвод и т.д.;
    - явный конфликт материала/ГОСТ/типа соединения блокирует замену.
  - [x] Зафиксировать правила в `docs/project.md`.
- **Зависимости**: Нет.
- **DoD**:
  - Правила реализованы в коде и покрыты тестами.
  - Есть короткий набор правил «можно / нельзя / требует подтверждения».
  - Подбор кандидатов и закупочный guard используют единый кодовый контракт.
  - Правила отражены в `docs/project.md`.
- **Факт**:
  - Добавлен `object.request.substitution.policy` с решениями
    `allowed_with_confirmation`, `blocked`, `unknown_requires_review`.
  - `matching_candidate_service` добавляет в payload кандидата
    `substitution_decision`, `substitution_reason`,
    `substitution_requires_confirmation`; `blocked` не получает складской
    бонус, а кандидаты с требованием подтверждения не проходят
    `auto_match_candidate()`.
  - Guard закупки и предупреждение строки пропускают `blocked`-кандидатов и
    выводят причину решения policy.
  - Ведение `allowed_substitute_ids` ограничено ролью
    `object_request.group_supply_manager` или системным администратором.
  - Добавлены focused-тесты в `test_obr028_combined_matching.py` и
    `test_obr017_security.py`.

---

## Этап 1. Быстрая стабилизация через память сопоставлений

- **Статус**: Выполнена
- **Описание**: Использовать уже существующую память сопоставлений, чтобы повторяющиеся строки автоматически предлагали правильную карточку товара.
- **Шаги выполнения**:
  - [x] Проверить текущую модель `object.request.matching.memory` и ограничения уникальности.
  - [x] Добавить или использовать UI-действие **«Запомнить сопоставление»** для строк требования.
  - [x] Кнопка **«Запомнить»** работает без `supplier_article`:
    создаёт запись в `object.request.matching.memory`, а supplierinfo
    пропускает.
  - [x] Для фланцев заполнить стартовую память сопоставлений по принципу:
    - `Фланец ст. Ду <N>мм 1,0МПа` → `Фланец DN<N>, PN 16`;
    - применять для всех найденных фланцев, где совпадает диаметр и нет
      явного конфликта материала, ГОСТ, исполнения или типа соединения;
    - если есть несколько сильных кандидатов одного диаметра, не создавать
      память автоматически, а вынести строку на ручное подтверждение.
  - [x] Проверить, что при новом импорте похожих строк память поднимает правильный товар в кандидаты.
  - [x] Добавить сообщение пользователю, что сопоставление сохранено и будет использоваться в будущих требованиях.
- **Зависимости**: Этап 0 желательно, но не блокирует.
- **DoD**:
  - Повторный импорт/пересопоставление строки с тем же нормализованным именем предлагает правильный товар.
  - Память не перезаписывает ручной защищённый выбор без действия пользователя.
- **Факт MVP-1**:
  - `action_remember_matching()` теперь сохраняет выбранный `product_id` в
    `object.request.matching.memory` по нормализованному `name_raw`.
  - Действие **«Запомнить»** доступно в UI без артикула поставщика; в этом
    случае создаётся только запись памяти сопоставлений.
  - Создание `product.supplierinfo` сохранено как дополнительный механизм при
    наличии корректного артикула и поставщика.
  - Добавлены тесты `TestObr032Memory.test_manual_remember_creates_memory_record`
    и `test_manual_remember_without_supplier_article_is_memory_only`.
- **Факт завершения**:
  - Добавлен идемпотентный backfill
    `object.request.matching.memory.backfill_flange_pn16_memory()`.
  - Backfill сканирует активные товары-фланцы, берёт источники `PN10` /
    `1,0МПа` и создаёт память только на единственный допустимый `PN16`
    того же диаметра по правилам `object.request.substitution.policy`.
  - Если по диаметру найдено несколько допустимых `PN16`-кандидатов или
    policy видит конфликт, запись памяти не создаётся.
  - Добавлена миграция `19.0.1.7.5`, выполняющая backfill при обновлении
    модуля.
  - Добавлены тесты на создание памяти для однозначного фланца и пропуск
    неоднозначного диаметра.

---

## Этап 2. Ранжирование кандидатов с учётом складских остатков

- **Статус**: Выполнена
- **Описание**: Доработать `object.request.matching.candidate.service`, чтобы кандидаты с остатком на складах выдачи получали повышенный приоритет.
- **Шаги выполнения**:
  - [x] Добавить в сервис кандидатов опциональный контекст требования/строки: `request`, `issue_warehouses`.
  - [x] Реализовать helper для расчёта доступного остатка кандидатов на выбранных складах выдачи.
  - [x] Добавить в candidate payload поля:
    - `stock_qty_on_issue_warehouses`;
    - `stock_warehouse_names`;
    - `has_issue_stock`;
    - `stock_rank_bonus`.
  - [x] Обновить сортировку кандидатов:
    - сначала точные совпадения из памяти;
    - затем кандидаты с остатком и сильным совпадением по `Ду/DN/PN`;
    - затем кандидаты без остатка;
    - затем слабые текстовые совпадения.
  - [x] Не поднимать товар с остатком выше, если есть конфликт диаметра или явный конфликт PN/материала.
  - [x] Добавить объяснение в candidate `reason`: «Есть остаток на складах выдачи: ...».
  - [x] Добавить в `matching_note` объяснение: «Есть остаток на Ос.ск: 201 шт».
  - [x] Обновить минимальные тесты MVP-1: `test_obr028_combined_matching.py`.
  - [x] Обновить полный набор тестов `test_obr027_matching.py`, `test_obr029_llm_matching.py`.
- **Зависимости**: Этап 0.
- **DoD**:
  - Для строки `Фланец ст. Ду 65мм 1,0МПа` кандидат `Фланец DN65, PN 16` показывается выше товара без остатка при совпадении параметров.
  - Кандидат с конфликтующим диаметром не попадает в верх выдачи только из-за остатка.
  - Ранжирование работает без N+1 запросов по `stock.quant`.
- **Факт MVP-1**:
  - `object.request.matching.candidate.service.build_candidates()` принимает
    `request` и `issue_warehouses` без нарушения старых вызовов.
  - Остатки считаются через один grouped-запрос по `stock.quant`.
  - Добавлен регрессионный тест
    `TestObr028CombinedMatching.test_candidate_service_ranks_issue_stock_candidate_higher`
    на кейс фланцев с остатком 201 шт.
- **Факт завершения**:
  - `action_prepare_ai_candidates()` передаёт контекст требования и склады
    выдачи в `matching_candidate_service`, поэтому shortlist получает
    складские остатки.
  - `_ai_candidate_result_vals()` добавляет в `matching_note` строку вида
    «Есть остаток на Ос.ск: 201 шт (...)».
  - Кандидаты, требующие подтверждения замены, не проходят массовое
    автоприменение: confidence ограничивается ниже `0.90`.
  - Обновлены тесты `test_obr027_matching.py`, `test_obr029_llm_matching.py`
    и добавлена проверка stock-note в `test_obr021_purchase.py`.

---

## Этап 3. Улучшение UI выбора товара в строке требования

- **Статус**: Выполнена
- **Описание**: Сделать выбор товара менее «слепым»: показать пользователю кандидатов, остатки и причину рекомендации до ручного выбора `product_id`.
- **Шаги выполнения**:
  - [x] На форме требования сделать колонку `AI shortlist` видимой или добавить отдельную вкладку/секцию **«Кандидаты подбора»**.
  - [x] В списке кандидатов показывать:
    - товар;
    - доступный остаток по выбранным складам;
    - склад остатка;
    - источник: память, артикул, имя, combined search, AI;
    - причину совпадения.
  - [x] Добавить кнопку **«Выбрать кандидата»** для строки, если кандидат имеет остаток и нет конфликтов.
  - [x] Добавить визуальное предупреждение рядом с `product_id`, если выбранный товар без остатка, но есть похожий кандидат с остатком.
  - [x] Для ручного `product_id` добавить проверку по действию:
    - если выбран товар без остатка;
    - и есть похожие товары с остатком;
    - заполнить предупреждающее поле/сообщение в строке.
  - [x] Реализовать live-onchange при ручном выборе `product_id`.
  - [x] Не блокировать ручной выбор на этом этапе, только информировать.
- **Зависимости**: Этап 2.
- **DoD**:
  - Пользователь видит `Фланец DN65, PN 16` как кандидата с остатком до отправки в закупку.
  - В строке понятно, почему товар рекомендуется.
  - UI не требует открывать карточку товара, чтобы понять наличие.
- **Факт MVP-2**:
  - В строках требования и общем списке строк видны `AI shortlist`,
    `Складской кандидат`, `Остаток кандидата` и текст предупреждения.
  - Добавлено действие **«Проверить номенклатуру»** на требовании и
    **«Проверить складской кандидат»** на строке.
  - Строки с `stock_match_warning=True` подсвечиваются warning-декорацией.
- **Факт завершения**:
  - Добавлено действие строки `action_select_stock_match_candidate()` и
    кнопка **«Выбрать кандидата»** в списках строк.
  - При выборе кандидат записывается в `product_id`, предупреждение очищается,
    а решение фиксируется в `matching_note`.
  - `@api.onchange('product_id')` сразу пересчитывает складское предупреждение
    при ручном выборе товара.

---

## Этап 4. Защитная проверка перед закупкой

- **Статус**: Выполнена
- **Описание**: Перед созданием PO не давать строке уйти в закупку без проверки, если есть похожий товар с остатком на складах выдачи.
- **Шаги выполнения**:
  - [x] В `object.request.purchase.wizard` добавить проверку строк `qty_to_buy > 0`.
  - [x] Для каждой строки найти похожих кандидатов с остатком через обновлённый candidate service.
  - [x] Если найден кандидат с остатком и выбранный товар без остатка, остановить создание PO и показать предупреждение в wizard:
    - строка требования;
    - выбранный товар;
    - кандидат с остатком;
    - остаток и склад;
    - действие MVP: пользователь исправляет строку или явно ставит флаг обхода.
  - [x] Полный blocking wizard с действиями **«Заменить на этот товар»**, **«Оставить закупку»**, **«Отмена»**.
  - [x] Добавить логирование решения в chatter требования:
    - кто подтвердил закупку несмотря на наличие похожего товара;
    - какой кандидат был отклонён.
  - [x] Разрешить снабженцу явно подтвердить закупку, если кандидат не подходит.
  - [x] Запретить создание PO без решения по предупреждению.
  - [x] Добавить тесты MVP-1:
    - кандидат с остатком блокирует создание закупки;
    - подтверждение «оставить закупку» создаёт PO и пишет chatter;
  - [x] Добавить тесты полного wizard:
    - «заменить на этот товар» переводит строку в выдачу после пересчёта;
    - строка без похожего остатка не блокируется.
- **Зависимости**: Этап 2.
- **DoD**:
  - Ошибка как с фланцем не проходит молча в PO.
  - Пользователь может осознанно продолжить закупку, если это действительно другой товар.
- **Факт MVP-1**:
  - Добавлен флаг `confirm_stock_guard_override` в wizard создания закупок.
  - Без флага PO не создаётся, если выбранный товар без остатка, но найден
    похожий кандидат с остатком на складах выдачи.
  - В MVP-2 обход стал двухшаговым: сначала wizard показывает список
    конфликтов, затем открывает флаг осознанного обхода.
  - При обходе предупреждения решение пишется в chatter требования.
  - Тесты: `test_purchase_guard_blocks_similar_product_with_issue_stock`,
    `test_purchase_guard_override_creates_po_and_logs_note`.
- **Факт завершения**:
  - Wizard закупки показывает действия **«Заменить на этот товар»**,
    **«Оставить закупку»** и **«Отмена»** после обнаружения складского
    кандидата.
  - **«Заменить на этот товар»** записывает складской кандидат в строку,
    пересчитывает наличие и переводит количество в выдачу вместо закупки.
  - **«Оставить закупку»** явно подтверждает обход guard-а и продолжает
    создание PO.
  - Добавлены тесты `test_purchase_guard_replace_candidate_moves_line_to_issue`
    и `test_purchase_guard_without_similar_stock_creates_po`.

---

## Этап 5. Каталог аналогов и допустимых замен

- **Статус**: Выполнена
- **Описание**: Перевести повторяющиеся замены из разовых предупреждений в управляемые правила аналогов через отдельную модель `object.request.product.substitute.rule`.
- **Шаги выполнения**:
  - [x] Создать модель `object.request.product.substitute.rule`:
    - `product_id` — исходный товар;
    - `substitute_product_id` — допустимый аналог;
    - `direction`: `one_way` / `two_way`;
    - `confirmation_policy`: например `always_confirm` на первом этапе;
    - `reason` — почему аналог допустим;
    - `note` — пояснение для снабженца;
    - `active`;
    - `company_id`;
    - `confirmed_by`;
    - `confirmed_date`;
    - опционально `usage_count`, `last_used_date` для будущей аналитики.
  - [x] Добавить ограничения и валидации модели:
    - запрет `product_id == substitute_product_id`;
    - уникальность пары в рамках компании и направления;
    - запрет дубля обратного правила для `two_way`;
    - проверка через Этап 0: правило нельзя создать, если базовая политика
      считает замену `blocked`;
    - архивирование через `active=False`, а не удаление.
  - [x] Добавить права доступа:
    - снабженец/админ может создавать и подтверждать правила;
    - прораб может только видеть применённый аналог/предупреждение, если это
      нужно в UI;
    - применение аналога из строки требования доступно только ответственной
      роли, не автоматически.
  - [x] Добавить UI управления аналогами:
    - меню/список правил аналогов;
    - форма правила с исходным товаром, аналогом, направлением и причиной;
    - smart-button или вкладку на карточке товара «Аналоги»;
    - фильтры `active`, `company_id`, `product_id`, `substitute_product_id`.
  - [x] Интегрировать каталог аналогов в строку требования:
    - добавить вычисляемые/служебные поля для найденного разрешённого аналога
      с остатком;
    - не смешивать остаток основного товара и аналога;
    - показывать отдельно «остаток основного товара» и «остаток аналога»;
    - использовать `allowed_substitute_ids` как отображение/результат найденных
      правил, но не как источник истины.
  - [x] Добавить действие **«Использовать аналог»** в строке требования:
    - действие доступно только для разрешённого правила аналога;
    - при применении пользователь явно подтверждает замену;
    - после подтверждения либо обновлять `product_id`, либо фиксировать
      `substitute_product_id` отдельным полем — выбрать один вариант и
      описать его в `docs/project.md`;
    - после применения пересчитать наличие/выдачу для выбранного товара.
  - [x] Интегрировать каталог в закупочный guard:
    - если строка уходит в закупку, но разрешённый аналог есть на складе,
      wizard должен предупреждать о разрешённом аналоге отдельно от просто
      похожих кандидатов;
    - закупка не создаётся без решения пользователя;
    - обход закупки несмотря на разрешённый аналог логируется в chatter.
  - [x] Добавить журналирование:
    - кто создал/подтвердил правило аналога;
    - кто применил аналог в строке требования;
    - какой товар был заменён на какой;
    - почему закупка продолжена, если разрешённый аналог был на складе.
  - [x] Добавить тесты:
    - однонаправленная замена работает только в разрешённую сторону;
    - двунаправленная замена работает в обе стороны;
    - правило с `blocked` по Этапу 0 нельзя создать;
    - разрешённый аналог с остатком показывается отдельно от основного товара;
    - действие **«Использовать аналог»** требует явного подтверждения и
      журналирует решение;
    - закупочный wizard блокирует строку, если есть разрешённый аналог с
      остатком;
    - прораб не может создать/подтвердить правило аналога без прав.
  - [x] Обновить `docs/project.md`, `docs/changelog.md` и при необходимости
    пользовательскую инструкцию.
- **Зависимости**: Этап 0, Этап 2.
- **DoD**:
  - Для типовых фланцев и других товарных семейств можно закрепить допустимую
    замену отдельным правилом.
  - Однонаправленные и двунаправленные правила работают предсказуемо.
  - Расчёт наличия видит остаток разрешённого аналога и предлагает выдачу
    вместо закупки, но не смешивает его с остатком основного товара.
  - Аналог не применяется автоматически: пользователь явно подтверждает
    замену, а решение журналируется.
  - Недопустимые похожие товары не попадают в каталог аналогов и не получают
    поведение разрешённого аналога.
- **Факт**:
  - Добавлена модель `object.request.product.substitute.rule` с направлением
    `one_way` / `two_way`, причиной, заметкой, компанией, подтверждением,
    `usage_count`, `last_used_date` и архивированием через `active`.
  - Правило нельзя создать для одинаковых товаров, для `blocked`-замены по
    `object.request.substitution.policy`, а для `two_way` дополнительно
    запрещён обратный активный дубль и проверяется обратное направление.
  - Каталог доступен через меню **Правила аналогов** и smart-button
    **Аналоги** на карточке товара; снабженец/админ ведут правила, прораб и
    кладовщик только читают.
  - Строка требования показывает разрешённый аналог и его остаток отдельно от
    `stock_qty_on_hand` выбранного товара; `allowed_substitute_ids`
    заполняется как отображение найденного правила.
  - Действие **«Использовать аналог»** явно заменяет `product_id`, очищает
    предупреждения, пересчитывает наличие/выдачу, увеличивает счётчик правила
    и пишет chatter-note.
  - Закупочный guard отдельно пишет «разрешённый аналог» для правил каталога и
    блокирует создание PO без решения пользователя; обход логируется.
  - Добавлены focused-тесты `test_obr036_substitute_rules.py` и ACL-регрессии
    в `test_obr017_security.py`.

---

## Этап 6. Нормализация параметров номенклатуры

- **Статус**: Выполнена
- **Описание**: Выделить технические признаки товара в структуру, чтобы поиск понимал `Ду 65`, `DN65`, `PN16`, `1,0МПа`, материал и ГОСТ.
- **Шаги выполнения**:
  - [x] Описать целевую схему признаков:
    - `diameter_nominal`;
    - `pressure_nominal`;
    - `material`;
    - `standard`;
    - `connection_type`;
    - `product_family`.
  - [x] Решить, где хранить признаки:
    - атрибуты Odoo;
    - отдельные поля на `product.template`;
    - отдельная модель классификации.
  - [x] Реализовать парсер признаков из названий для пилотных категорий:
    - фланцы;
    - прокладки;
    - переходы;
    - отводы;
    - краны.
  - [x] Добавить команду/скрипт аудита справочника:
    - найти дубли по `family + diameter + pressure`;
    - найти товары без `DN/Ду`;
    - найти конфликтующие названия.
  - [x] Добавить отчёт для ручной чистки номенклатуры.
  - [x] Не переименовывать товары автоматически без подтверждения пользователя.
- **Зависимости**: Этап 0.
- **DoD**:
  - Для фланцев система понимает, что `Ду 65`, `DN65`, `65мм` — один номинальный диаметр.
  - Поиск кандидатов использует признаки, а не только текстовое совпадение.
  - Есть отчёт по потенциальным дублям справочника.
- **Факт**:
  - Добавлен `object.request.product.feature.parser` с целевой схемой:
    `product_family`, `diameter_nominal`, `pressure_nominal`, `material`,
    `standard`, `connection_type`.
  - Признаки хранятся в stored-полях `product.template` и related stored-полях
    `product.product`; атрибуты Odoo не используются, чтобы не смешивать
    техническую нормализацию с торговыми вариантами.
  - Parser поддерживает пилотные семейства: фланцы, прокладки, переходы,
    отводы, краны/клапаны и трубы; распознаёт `Ду/DN/мм`, `PN/Ру/МПа`,
    материал, ГОСТ и тип соединения.
  - `matching_candidate_service` добавляет источник `feature` и ищет по
    `family + DN + PN>=requested`; конфликт семейства/DN отсекается, а
    конфликт PN остаётся `blocked` по policy.
  - Добавлен отчёт `object.request.product.feature.audit.line`, доступный из
    меню **Аудит номенклатуры**, и shell-скрипт
    `scripts/product_feature_audit.py`.
  - Товары не переименовываются автоматически; признаки только заполняются и
    показываются для ручной чистки.
  - Добавлены тесты `test_obr037_product_features.py`.

---

## Этап 7. Контроль качества справочника перед импортом и в строках

- **Статус**: Частично выполнена (MVP-2)
- **Описание**: Добавить проверки качества выбора товара в импорт и обработку требования.
- **Шаги выполнения**:
  - [ ] При импорте Excel помечать строки `manual_review`, если:
    - найдено несколько сильных кандидатов;
    - выбранный кандидат без остатка, а похожий кандидат с остатком;
    - есть конфликт `Ду/DN/PN`.
  - [x] Добавить фильтр **«Есть похожий товар на складе»**.
  - [x] Добавить счётчик проблемных строк в шапке требования.
  - [ ] Запретить создание закупки, если есть нерешённые критические предупреждения по номенклатуре.
  - [x] Добавить массовое действие **«Проверить складские аналоги»**.
  - [ ] Добавить массовое действие **«Переподобрать с учётом остатков»**.
- **Зависимости**: Этап 2, Этап 4.
- **DoD**:
  - Ошибочные строки видны до расчёта наличия.
  - Снабженец может быстро отфильтровать и исправить спорные позиции.
- **Факт MVP-2**:
  - `line_problem_count` учитывает `stock_match_warning`.
  - Smart-button **«Проблемы»** открывает строки с ошибками сопоставления,
    поставщика или складского кандидата.
  - В search view добавлен фильтр **«Есть похожий товар на складе»**.

---

## Этап 8. Улучшение алгоритма автоподбора и AI-кандидатов

- **Статус**: Не начата
- **Описание**: Сделать AI/combined-подбор более полезным для складских сценариев.
- **Шаги выполнения**:
  - [ ] Передавать в LLM/combined context:
    - исходное имя строки;
    - техническое обозначение;
    - список top-N кандидатов;
    - остатки по складам выдачи;
    - признаки `DN/PN/материал`.
  - [ ] В `ai_match_reason` добавлять объяснение, почему кандидат выбран или отклонён.
  - [ ] Для уверенных совпадений с остатком повышать confidence, если нет конфликтов.
  - [ ] Для совпадений без остатка понижать confidence, если есть равноценный кандидат с остатком.
  - [ ] Защитить от авто-применения при конфликте давления/диаметра.
  - [ ] Добавить тесты на типовой кейс фланцев.
- **Зависимости**: Этап 2, Этап 6.
- **DoD**:
  - AI shortlist предлагает складской товар, когда он технически подходит.
  - AI не автоприменяет спорную замену без ручного подтверждения.

---

## Этап 9. UX-сценарий исправления уже созданной закупки

- **Статус**: Частично выполнена (MVP-2)
- **Описание**: Дать пользователю понятный путь, если ошибка обнаружена после создания черновика PO.
- **Шаги выполнения**:
  - [x] Добавить действие на требовании **«Проверить закупки на складские аналоги»**.
  - [x] Проверять строки, связанные с PO:
    - `purchase_order_id`;
    - `purchase_order_line_id`;
    - текущий `product_id` строки требования;
    - товар в строке PO.
  - [x] Если товар строки требования и товар PO отличаются, показать предупреждение.
  - [ ] Если PO в черновике, предложить:
    - удалить строку из PO;
    - заменить товар в PO;
    - вернуть строку в выдачу;
    - оставить как есть.
  - [ ] Если PO уже подтверждён, показать безопасную инструкцию без автоматического удаления.
  - [x] Добавить chatter-запись о найденных предупреждениях.
- **Зависимости**: Этап 4.
- **DoD**:
  - P00068-подобная ситуация диагностируется в один клик.
  - Для черновика PO есть безопасный UI-путь исправления.
- **Факт MVP-2**:
  - Smart-button **«Диагностика PO»** на требовании запускает диагностику уже
    связанных строк закупки.
  - Если товар в строке PO отличается от текущего товара требования, строка
    получает складское предупреждение с текстом расхождения.
  - При найденных предупреждениях открывается список строк требования и
    пишется chatter-note.
  - Автоматическое удаление/замена строк PO пока не выполняется.

---

## Этап 9.1. Упрощение UI сценария требования

- **Статус**: Выполнена (MVP-2)
- **Описание**: Убрать из основного UI лишние и дублирующие кнопки, чтобы пользователь шёл по безопасному сценарию: сопоставить → проверить номенклатуру → рассчитать наличие → авто-разбивка → выдача/закупка.
- **Шаги выполнения**:
  - [x] Убрать из верхней панели вкладки **Строки** кнопку **«Закупить всё»**:
    - оставить действие только как массовое server action в списке строк;
    - добавить/проверить `confirm`, потому что действие обходит складской сценарий.
  - [x] Убрать из верхней панели вкладки **Строки** кнопку **«Выдать максимум»**:
    - оставить как расширенное массовое действие;
    - основной сценарий должен идти через **«Рассчитать наличие»** и **«Авто-разбивка»**.
  - [x] Убрать из верхней панели вкладки **Строки** кнопку **«Сбросить разбивку»**:
    - оставить только как массовое действие или показывать при наличии распределения;
    - добавить `confirm`, если действие остаётся доступным.
  - [x] Переименовать построчную кнопку **«Проверить склад»**:
    - новый текст: **«Проверить аналог»** или **«Проверить складской кандидат»**;
    - цель: не путать с полноценным **«Рассчитать наличие»**.
  - [x] Оставить построчную проверку складского кандидата только для снабженца и только в расширенном/проблемном списке строк:
    - в основной таблице требования достаточно кнопки **«Проверить номенклатуру»** по всему документу;
    - если кнопка остаётся в строке, она должна быть вторичным действием.
  - [x] Перенести из header в отдельный блок **«Сопоставление номенклатуры»**:
    - **«Пересопоставить несопоставленные»**;
    - **«Пересопоставить все строки»**;
    - **«Подобрать AI-кандидатов»**;
    - **«Применить уверенные AI-сопоставления»**.
  - [x] Для **«Применить уверенные AI-сопоставления»** добавить более явное подтверждение:
    - указать, что это массовая запись товаров в строки;
    - не размещать рядом с кнопками жизненного цикла документа.
  - [x] Переименовать smart-button **«Проверить PO»**:
    - вариант 1: **«Проверить закупки»**;
    - вариант 2: **«Диагностика PO»**;
    - цель: объяснить, что действие проверяет расхождения и складские аналоги, а не просто открывает PO.
  - [x] В wizard закупки убрать ранний чекбокс **«Закупить несмотря на похожий остаток»** из первого экрана:
    - первая попытка создания PO должна показать список конфликтов;
    - обход должен появляться только после обнаружения предупреждений;
    - решение обхода должно логироваться в chatter.
  - [x] Проверить дубли между inline-кнопками строк и server actions:
    - `Запомнить`;
    - `Проверить складские аналоги`;
    - `Закупить всё`;
    - `Выдать максимум`;
    - `Сбросить разбивку`.
  - [x] Сформировать правило размещения действий:
    - header — только жизненный цикл документа и основные документы;
    - вкладка **Обработка** — основной поток обработки;
    - вкладка/блок **Сопоставление** — AI и пересопоставление;
    - server actions — массовые/опасные операции;
    - строка — только точечные безопасные действия.
  - [x] Обновить help/title у оставшихся кнопок на русском языке.
  - [x] Добавить тест/проверку view-архитектуры, что опасные кнопки не видны в основном блоке.
- **Зависимости**: Этапы 3, 4, 7, 9.
- **DoD**:
  - В header не осталось массовых действий сопоставления.
  - На вкладке **Строки** нет кнопок, которые напрямую переводят всё в закупку/выдачу без основного сценария.
  - Основной путь визуально читается как: **Проверить номенклатуру → Рассчитать наличие → Авто-разбивка → Создать выдачу / Подготовить закупку**.
  - Обход закупочного guard-а нельзя включить до просмотра предупреждения.
  - Smart-button диагностики PO имеет понятное название.
- **Факт MVP-2**:
  - Header формы требования очищен от массовых действий сопоставления.
  - На вкладке **Строки** больше нет прямых кнопок **«Закупить всё»**,
    **«Выдать максимум»**, **«Сбросить разбивку»**.
  - Действия пересопоставления и AI перенесены во вкладку
    **«Сопоставление»** / блок **«Сопоставление номенклатуры»**.
  - Smart-button **«Проверить PO»** переименован в **«Диагностика PO»**.
  - Wizard закупки стал двухшаговым: при найденных складских кандидатах
    сначала показывает список конфликтов, и только после этого показывает
    флаг осознанного обхода.

---

## Этап 10. Тестирование

- **Статус**: Выполнена для промежуточного релиза
- **Описание**: Покрыть ключевые сценарии, чтобы защита не сломала текущий цикл требований, выдач и закупок.
- **Шаги выполнения**:
  - [ ] Unit-тесты candidate service:
    - ранжирование по остаткам;
    - конфликт диаметра;
    - конфликт PN;
    - память сопоставлений;
    - аналоги.
  - [ ] Тесты импорта:
    - строка с несколькими кандидатами;
    - строка с похожим товаром на складе;
    - строка без подходящего кандидата.
  - [ ] Тесты закупочного wizard:
    - блокировка при наличии похожего складского товара;
    - подтверждение закупки несмотря на предупреждение;
    - замена на складской товар;
    - создание PO без предупреждений.
  - [ ] Тесты выдачи:
    - выдача основного товара;
    - выдача подтверждённого аналога;
    - запрет автоматической выдачи спорного аналога.
  - [ ] Регрессионные тесты текущих сценариев:
    - `test_obr019_stock_check.py`;
    - `test_obr021_purchase.py`;
    - `test_obr024_warehouse.py`;
    - `test_obr025_multiwarehouse_check.py`;
    - `test_obr027_matching.py`;
    - `test_obr028_combined_matching.py`;
    - `test_obr029_llm_matching.py`;
    - `test_obr034_issue_warehouse_filter.py`;
    - `test_obr035_regressions.py`.
- **Зависимости**: Все функциональные этапы.
- **DoD**:
  - Все тесты модуля `object_request` проходят.
  - Есть отдельный регрессионный тест на кейс «Фланец 65-10 кованый» vs «Фланец DN65, PN 16».
- **Факт перед merge 2026-07-02**:
  - `flake8 /mnt/extra-addons/object_request` прошёл без ошибок.
  - Полный `/object_request` прошёл: 395 post-tests,
    0 failed, 0 errors.
  - Focused stock guards прошли:
    `TestOBR021Purchase`, `TestObr032Memory`,
    `TestObr028CombinedMatching`, `TestObr009MassActions` — 80 post-tests,
    0 failed, 0 errors.
  - Для промежуточного релиза исправлены регрессии test suite:
    уникальность кодов проектных складов в локальной/тестовой базе,
    авторазбивка по разрешённым складам выдачи и валидация ручного
    превышения складского остатка.

---

## Этап 11. Документация и обучение пользователей

- **Статус**: Частично выполнена
- **Описание**: Обновить проектную документацию и инструкции для прораба/снабженца.
- **Шаги выполнения**:
  - [ ] Обновить `docs/project.md`:
    - схема сопоставления товаров;
    - роль памяти сопоставлений;
    - роль аналогов;
    - защита перед закупкой.
  - [x] Обновить `docs/changelog.md` после внедрения каждого user-visible изменения.
  - [ ] Подготовить короткую инструкцию:
    - как выбирать товар из кандидатов;
    - что значит предупреждение «есть похожий товар на складе»;
    - как подтвердить аналог;
    - когда всё равно создавать закупку.
  - [ ] Добавить troubleshooting-раздел:
    - товар ушёл в закупку, хотя есть на складе;
    - в PO товар отличается от строки требования;
    - кандидат не найден.
- **Зависимости**: Этапы 2-9.
- **DoD**:
  - Пользователь понимает, что делать при предупреждении.
  - Документация отражает фактическое поведение UI.

---

## Этап 12. Миграция и внедрение

- **Статус**: Не начата
- **Описание**: Безопасно включить новые проверки на существующей базе.
- **Шаги выполнения**:
  - [ ] Сделать аудит существующих строк требований со статусом `buy`, где есть похожие товары с остатком.
  - [ ] Сформировать список потенциально ошибочных закупок.
  - [ ] Заполнить стартовые записи памяти сопоставлений для подтверждённых кейсов.
  - [ ] Заполнить стартовые правила аналогов для утверждённых замен.
  - [ ] Включить warning-only режим для первой итерации.
  - [ ] После проверки пользователями включить blocking режим перед закупкой.
  - [ ] Зафиксировать дату переключения режима в `docs/changelog.md`.
- **Зависимости**: Этапы 4-5.
- **DoD**:
  - Новые проверки не ломают текущие требования.
  - Пользователи сначала видят предупреждения, затем блокировки включаются осознанно.

---

## Приоритеты реализации

### MVP-1: Быстрая защита

- [x] Этап 1. Память сопоставлений — MVP-вариант.
- [x] Этап 2. Ранжирование кандидатов по остаткам — MVP-вариант.
- [x] Этап 4. Предупреждение перед закупкой — MVP-вариант.
- [x] Этап 10. Минимальные тесты на фланцы.

**Цель MVP-1:** кейс с фланцем больше не уходит в закупку молча.

**Статус MVP-1 на 2026-06-30:** выполнен в минимальном варианте.
Кандидат с остатком поднимается в shortlist, закупка блокируется без явного
решения снабженца, обход предупреждения журналируется в chatter.

### MVP-2: Удобный UI

- [x] Этап 3. UI кандидатов и остатков — MVP-вариант.
- [x] Этап 7. Фильтры проблемных строк — MVP-вариант.
- [x] Этап 9. Диагностика уже созданных PO — MVP-вариант.
- [x] Этап 9.1. Упрощение UI сценария требования.

**Цель MVP-2:** снабженец видит и исправляет спорные строки без технических проверок.

**Статус MVP-2 на 2026-07-02:** выполнен в расширенном варианте.
Снабженец может пересчитать складские предупреждения, отфильтровать спорные
строки, увидеть складского кандидата, проверить уже созданные PO и пройти
по более чистому сценарию без опасных массовых кнопок в основном UI.

### MVP-3: Управляемые аналоги

- [ ] Этап 5. Каталог аналогов.
- [ ] Этап 6. Нормализация параметров.
- [ ] Этап 8. Улучшенный AI/combined-подбор.

**Цель MVP-3:** система устойчиво работает с разными названиями и допустимыми заменами.

---

## После MVP-3. Вкладка «Все поступления» в требовании

- **Статус**: Не начата
- **Описание**: Добавить в форму требования отдельную вкладку
  **«Все поступления»**, где снабженец видит все складские поступления,
  созданные по закупкам, связанным с данным требованием.
- **Шаги выполнения**:
  - [ ] Определить связи поступлений с требованием:
    - через строки требования `purchase_order_id` / `purchase_order_line_id`;
    - через `purchase.order`, созданные wizard-ом требования;
    - через `stock.picking` типа incoming, связанные с этими закупками.
  - [ ] Добавить вычисляемое поле/метод на `object.request`, которое собирает
    все связанные `stock.picking` поступлений без дублей.
  - [ ] Добавить вкладку **«Все поступления»** в форму требования.
  - [ ] В списке поступлений показать:
    - номер поступления;
    - статус;
    - поставщика;
    - связанный заказ закупки;
    - плановую/фактическую дату;
    - склад/тип операции;
    - ответственного;
    - кнопку/ссылку открытия поступления.
  - [ ] Не смешивать поступления по другим требованиям, даже если поставщик
    или закупка похожи.
  - [ ] Добавить тесты:
    - поступления по закупкам текущего требования отображаются во вкладке;
    - поступления по чужому требованию не отображаются;
    - несколько PO и несколько receipt picking отображаются без дублей;
    - отменённые/завершённые поступления видны со своим статусом.
  - [ ] Обновить `docs/project.md` и `docs/changelog.md`.
- **Зависимости**: Этапы 4 и 9, так как вкладка опирается на корректные связи
  требования со строками закупок и диагностикой PO.
- **DoD**:
  - На форме требования есть вкладка **«Все поступления»**.
  - Пользователь видит полный список receipt picking по закупкам этого
    требования без перехода в меню складских операций.
  - Вкладка не показывает поступления по другим требованиям.

---

## Открытые вопросы

- [ ] Считать ли `1,0МПа` эквивалентом `PN10`, а `PN16` более прочным допустимым аналогом?
- [ ] Можно ли заменить `кованый` фланец на `DN/PN` фланец без отдельного подтверждения?
- [ ] Кто утверждает справочник аналогов?
- [ ] Должна ли закупка блокироваться полностью или достаточно предупреждения на первом этапе?
- [ ] Нужно ли учитывать остатки только на складах выдачи или также на складах объекта/резервных складах?
- [ ] Как поступать, если подходящий аналог есть, но на другом складе, не выбранном в требовании?

## Риски и контроль

- **Риск:** система предложит похожий, но технически неподходящий товар.
  **Контроль:** не делать автоматическую замену без подтверждения; учитывать `Ду/DN/PN/материал`.

- **Риск:** блокировка закупки начнёт мешать срочным закупкам.
  **Контроль:** сначала warning-only режим, затем blocking режим с правом снабженца подтвердить закупку.

- **Риск:** ранжирование по остаткам поднимет неправильный товар только потому, что он есть на складе.
  **Контроль:** остаток — только бонус к скорингу, а не замена технического совпадения.

- **Риск:** каталог аналогов станет хаотичным.
  **Контроль:** отдельные права, журналирование, регулярный аудит правил.

- **Риск:** нормализация справочника затронет отчёты и документы.
  **Контроль:** не переименовывать массово без отдельного плана миграции и согласования.

## Команды проверки

```bash
docker exec odoo19-local odoo --test-enable -u object_request -d odoo19_local --stop-after-init
docker exec odoo19-local odoo --test-enable --test-tags /object_request -d odoo19_local --stop-after-init
docker exec odoo19-local python -m flake8 /mnt/extra-addons/object_request
```

## Критерии готовности всей инициативы

- При выборе спорного товара пользователь видит кандидата с остатком на складе.
- Перед созданием закупки система предупреждает о похожем товаре с остатком.
- Закупка не создаётся молча, если строка похожа на складской товар.
- Для утверждённых замен работает каталог аналогов.
- Память сопоставлений переиспользует правильные решения.
- Есть регрессионный тест на кейс фланцев `65-10 кованый` vs `DN65, PN16`.
- Документация и changelog обновлены.
