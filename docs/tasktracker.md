## Задача: Улучшенный поиск товаров custom_product_search
- **Статус**: Завершена
- **Описание**: Реализовать отдельный addon `custom_product_search` для нормализованного поиска товаров в Odoo 19: backend UI, складские документы и сервисный метод для AI-ассистента.
- **Шаги выполнения**:
 - [x] Проверить `docs/odoo_product_search_cursor.md` и актуальный Odoo 19 API поиска товаров.
 - [x] Создать подробный трекер `docs/tasktrecker-odoo-search.md`.
 - [x] Создать структуру addon в `custom_addons/custom_product_search`.
 - [x] Добавить normalized stored indexed поля `x_search_name`.
 - [x] Расширить `product.product.name_search()` с fallback по нормализованным полям.
 - [x] Добавить `post_init_hook` для `pg_trgm` и GIN индексов.
 - [x] Добавить `ai_search_products()` для AI-ассистента.
 - [x] Добавить README и тестовые сценарии.
 - [x] Выполнить проверки синтаксиса/линтера/Odoo tests.
- **Зависимости**:
 - Стандартные модули Odoo: `product`, `stock`.
 - PostgreSQL extension `pg_trgm`.
 - Детальный план: `docs/tasktrecker-odoo-search.md`.

## Задача: Odoo 19 Docker workflow (Cursor → VPS → deploy)
- **Статус**: Завершена
- **Описание**: Подготовить документацию и готовую конфигурацию для запуска Odoo 19 в Docker локально (Cursor/WSL2) и на VPS (Ubuntu 22.04) с доменом `skladtsk.duckdns.org`, хранением данных в volumes и деплоем изменений через `git pull` + рестарт контейнера.
- **Шаги выполнения**:
 - [x] Описать структуру проекта и правила работы (код в `custom_addons`, конфиги/секреты вне git).
 - [x] Подготовить готовый `docker-compose.yml` (Traefik + Odoo + Postgres) под домен и HTTPS.
 - [x] Подготовить шаблон `config/odoo.conf` и правила `proxy_mode` за reverse proxy.
 - [x] Добавить команды первого запуска, обновления кода и upgrade модулей (`-u`).
 - [x] Добавить базовые команды логов и бэкапов (pg_dump + filestore volume).
 - [x] Клонировать исходники Odoo `19.0` в `odoo/`.
 - [x] Подготовить локальный запуск на `localhost:8069` (контейнеры Postgres+Odoo, volumes).
 - [x] Инициализировать базу `odoo19_local` модулем `base` для корректного старта веб-интерфейса.
- **Зависимости**:
 - DNS A-запись для `skladtsk.duckdns.org` на IP VPS
 - Открытые порты 80/443 на VPS
 - Docker + Docker Compose на локальной машине и на VPS

## Задача: План AI-консультанта для Odoo через OpenRouter
- **Статус**: Завершена
- **Описание**: Подготовить детальный план реализации встроенного AI-ассистента для Odoo 19 в виде чата в правом нижнем углу, с хранением истории только в браузерной сессии и ответами на основе документации Odoo.
- **Шаги выполнения**:
 - [x] Определить целевую архитектуру модуля `ai_assistant`.
 - [x] Спроектировать UI чата (floating widget / bottom-right panel).
 - [x] Зафиксировать стратегию хранения истории в `sessionStorage`.
 - [x] Описать backend proxy для OpenRouter и безопасное хранение API key.
 - [x] Описать knowledge-base подход на основе curated documentation pack по Odoo.
 - [x] Разбить реализацию на этапы и подзадачи (`AIA-001` ... `AIA-011`).
 - [x] Сохранить план в `tasktreckeragentconsul.md`.
- **Зависимости**:
 - OpenRouter API key
 - Кастомный модуль Odoo в `custom_addons/`
 - Документация Odoo 19 для подготовки knowledge pack

## Задача: Roadmap модуля "Требование на комплектацию объекта"
- **Статус**: Завершена
- **Описание**: Подготовить roadmap для кастомного модуля Odoo 19, который покрывает процесс требования на комплектацию объекта: импорт Excel, ручное сопоставление строк, частичную выдачу со склада, печатные формы и подготовку дефицита к закупке.
- **Шаги выполнения**:
 - [x] Зафиксировать бизнес-правила по документу, ролям, Excel-импорту и статусам.
 - [x] Описать целевой workflow от прораба до снабженца и кладовщика.
 - [x] Определить состав шапки документа и модели строк.
 - [x] Разделить scope на MVP и этап 2.
 - [x] Описать опору на стандартные механизмы Odoo (vendor info, stock, purchase).
 - [x] Использовать Context7 для подтверждения стандартных Odoo-паттернов складов и закупок.
 - [x] Сохранить roadmap в `docs/roadmapobjectrequest.md`.
- **Зависимости**:
 - Наличие базовых справочников: объекты, товары, поставщики
 - Стандартные модули Odoo: Inventory, Purchase, Contacts
 - Уточнение выбора модели объекта (`project.project` или отдельная модель)

## Задача: Functional spec экранов и кнопок для требования на комплектацию объекта
- **Статус**: Завершена
- **Описание**: Подготовить отдельный functional spec по экранным формам и кнопкам для будущего кастомного Odoo 19 модуля `Требование на комплектацию объекта`, чтобы зафиксировать UI-структуру, поведение форм, кнопок, ролей и сценариев пользователя.
- **Шаги выполнения**:
 - [x] Описать список экранов модуля и их назначение.
 - [x] Зафиксировать layout формы документа, список полей, smart buttons и tabs.
 - [x] Зафиксировать кнопки header и правила их видимости по статусам.
 - [x] Описать таблицу строк документа и inline-действия.
 - [x] Описать wizard импорта Excel.
 - [x] Описать форму выдачи и печатные формы.
 - [x] Зафиксировать UI-права по ролям: прораб, снабженец, кладовщик, согласующий.
 - [x] Сохранить functional spec в `docs/functionalspecobjectrequest.md`.
- **Зависимости**:
 - `docs/roadmapobjectrequest.md`
 - Утвержденные бизнес-правила по ролям и статусам
 - Понимание целевого UX для MVP и этапа 2

## Задача: Data model spec для требования на комплектацию объекта
- **Статус**: Завершена
- **Описание**: Подготовить отдельный data model spec для будущего кастомного Odoo 19 модуля `Требование на комплектацию объекта`, чтобы зафиксировать состав моделей, поля, типы, связи со стандартными моделями Odoo, ограничения и вычисляемые поля.
- **Шаги выполнения**:
 - [x] Выбрать рекомендуемый подход к модели объекта для MVP.
 - [x] Описать основные модели: объект, шапка требования, строки.
 - [x] Описать transient wizard-модели для импорта, выдачи и закупки.
 - [x] Описать расширения `stock.picking` и `purchase.order`.
 - [x] Зафиксировать required fields, связи, compute fields и constraints.
 - [x] Заложить поля под этап 2 без миграции структуры.
 - [x] Сохранить spec в `docs/datamodelspecobjectrequest.md`.
- **Зависимости**:
 - `docs/roadmapobjectrequest.md`
 - `docs/functionalspecobjectrequest.md`
 - Стандартные Odoo-модули: `stock`, `purchase`, `product`, `contacts`, `mail`

## Задача: Поле «Склад» в заявке на комплектацию
- **Статус**: Завершена
- **Описание**: Добавить обязательное поле `warehouse_id` (Склад) в модель `object.request`. Склад должен указываться при создании заявки (вручную и через импорт Excel), использоваться при расчёте наличия (`action_check_stock`), подставляться в wizard выдачи и передаваться в `purchase.order` при создании закупок (чтобы приёмка шла на правильный склад).
- **Шаги выполнения**:
  - [x] Добавить поле `warehouse_id` в модель `object.request`, исправить `action_check_stock`
  - [x] Добавить поле `warehouse_id` в wizard импорта Excel, передать в созданный `object.request`
  - [x] Исправить `issue_wizard.py` — брать склад из требования
  - [x] Исправить `purchase_wizard.py` — передавать `picking_type_id` склада в `purchase.order`
  - [x] Обновить представления: форма, список, wizard импорта
  - [x] Написать тесты
  - [x] Обновить модуль в Odoo, проверить работу
- **Зависимости**:
  - Модуль `object_request`, стандартный модуль `stock`

## Задача: Проверка наличия по нескольким складам в комплектации объектов
- **Статус**: Завершена
- **Описание**: Добавить возможность выбора нескольких складов для расчёта остатков. При нажатии «Рассчитать наличие» остатки суммируются по всем выбранным складам. Если список пуст — fallback на склад документа. Если найдены позиции на складе — открывается wizard с предупреждением и кнопкой «Проверено».
- **Шаги выполнения**:
  - [x] Добавить `check_warehouse_ids` (M2M) и `stock_check_confirmed` в модель
  - [x] Переписать `action_check_stock` — суммирование по складам, fallback, открытие wizard
  - [x] Создать wizard `object.request.stock.check.wizard` с кнопкой «Проверено»
  - [x] Добавить права доступа (ir.model.access.csv)
  - [x] Обновить представления форм
  - [x] Написать тесты (13 тестов, 260 всего, 0 failed)
- **Зависимости**: Задача «Поле «Склад» в заявке на комплектацию»


- **Статус**: В процессе
- **Описание**: На форме `object.request` на вкладке «Обработка» кнопки «Рассчитать наличие», «Авто-разбивка», «Подготовить закупку» отображаются разного размера и визуально «слипаются». Нужно привести к единому размеру и добавить корректные отступы, чтобы UI выглядел аккуратно.
- **Шаги выполнения**:
 - [ ] Проверить текущую разметку в `custom_addons/object_request/views/object_request_views.xml` (группа «Автоматическая обработка»).
 - [ ] Привести кнопки к единому стилю (одинаковый класс/размер), поправить spacing (margin/gap), при необходимости обернуть в `o_row`/`d-flex` контейнер с `gap-*`.
 - [ ] Проверить отображение в web-клиенте Odoo под admin/снабженцем.
- **Зависимости**:
 - Модуль `object_request` установлен и обновлён в базе.


